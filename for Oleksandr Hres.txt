Я не знаток  Python , але я так собі уявляю:

print("#9")
print(f"Anna has {a} apples and {p} peaches.".format(a, p))

 - на скільки я розумію метод format() в цьому завданні лишній, просто f"" .



 print("#22")
lst_to_sort = list(map(lambda elem: elem * 2, lst_to_sort))
print(lst_to_sort) 

- по суті все правильно, але як я собі уявляю завдання: ти помножив відсортований список, а не початковий.

# 26 1
print("#26 1")


def myfunc(lst_1, lst_2):
    temp_list = []
    for num in range(len(lst_1) - 1):
        for num2 in range(len(lst_1) - 1):
            if lst_1[num] == lst_2[num2]:
                temp_list.append(lst_1[num])
            else:
                pass
    return temp_list

- все правильно для даних списків, але якщо підставляти невідомі списки і у них один з останніх елементів буде
  співпадати, то ця функція не спрацює бо у тебе  "range(len(lst_1) - 1)"
